{
  "name": "charmer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "run-s clean build:**",
    "clean": "rm -rf dist",
    "build:index": "rsbuild build --env-mode=index",
    "build:main": "rsbuild build --env-mode=main",
    "build:view": "rsbuild build --env-mode=view",
    "dev": "rsbuild dev --env-mode=view --open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rsbuild/core": "^0.6.8",
    "@rspack/core": "^0.6.3",
    "@types/node": "^20.12.7",
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "npm-run-all": "^4.1.5",
    "simple-git": "^3.24.0"
  },
  "dependencies": {
    "localforage": "^1.10.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
